---
# Archive

Stuff from the previous version of the site (pre-Astro rewrite) kept around because it might come back.

- `d3-spider-chart.svg` — skills radar chart from the old "Developer Profile" trading-card page. Currently a static SVG; if reused, regenerate from D3 with the new design tokens so colors stay in sync with the theme.
- `legacy-index.html` / `legacy-style.css` — full snapshot of the old single-page site for reference (NBA Top Shot / trading-card framing, social icons row, etc).

These files are served at `/archive/*` but not linked from the new site.
